# Core deep-learning stack, as lower bounds. Deliberately not pinned to exact
# versions: the same code runs on Colab's managed GPU stack and on a local
# machine (incl. Apple-Silicon MPS builds), and exact pins would force Colab to
# reinstall a PyTorch it already has.
torch>=2.6
torchvision>=0.21
numpy>=1.26,<3
albumentations>=1.4
opencv-python-headless>=4.9
scikit-learn>=1.4
matplotlib>=3.8
tqdm>=4.66
pytest>=8.0

# Optional: dataset download on Colab
# kaggle>=1.6
